# Teacher Portfolio Template

เทมเพลตเว็บไซต์แฟ้มสะสมผลงานครู (Teacher Portfolio) สไตล์ Modern สวยงาม รองรับมือถือ พร้อมระบบจัดการหลังบ้าน (Admin Dashboard)

## ฟีเจอร์หลัก
- 🌐 **หน้าเว็บไซต์ Portfolio**: แสดงผลงาน กิจกรรม เกียรติบัตร และการประเมิน PA
- 📱 **Responsive Design**: รองรับการแสดงผลบนมือถือและแท็บเล็ต
- 🛠 **ระบบหลังบ้าน (Admin)**: แก้ไขข้อมูล อัปโหลดรูปภาพ และจัดการเนื้อหาได้ด้วยตัวเอง ไม่ต้องแก้โค้ด
- 💾 **Database**: เชื่อมต่อฐานข้อมูล Supabase เก็บข้อมูลปลอดภัย

## การติดตั้งและใช้งาน (สำหรับผู้เริ่มต้น)

### 1. เตรียมเครื่องมือ
- ติดตั้ง **Node.js** (https://nodejs.org)
- ติดตั้ง **VS Code** (https://code.visualstudio.com)
- สมัครสมาชิก **Supabase** (https://supabase.com) เพื่อใช้เป็นฐานข้อมูลฟรี

### 2. ตั้งค่า Supabase (ฐานข้อมูล)
1. สร้าง Project ใหม่ใน Supabase
2. ไปที่เมนู **SQL Editor**
3. นำโค้ดจากไฟล์ `setup.sql` ในโปรเจคนี้ ไปวางและกด **Run** เพื่อสร้างตารางและตั้งค่าระบบ
4. ไปที่เมนู **Settings -> API** และคัดลอก:
   - Project URL
   - anon public key

### 3. ตั้งค่าโปรเจค
1. เปิดโฟลเดอร์โปรเจคด้วย VS Code
2. เปลี่ยนชื่อไฟล์ `.env.example` เป็น `.env`
3. นำค่า URL และ Key ที่ได้จาก Supabase มาใส่ในไฟล์ `.env`
   ```
   VITE_SUPABASE_URL=https://your-project.supabase.co
   VITE_SUPABASE_PUBLISHABLE_KEY=your-anon-key-here
   ```

### 4. รันโปรเจค
เปิด Terminal ใน VS Code และพิมพ์คำสั่ง:
```bash
npm install
npm run dev
```
จากนั้นเปิด Browser ไปที่ `http://localhost:8080` (หรือ Port ที่แสดง)

### 5. การเข้าสู่ระบบ Admin
- ไปที่ `/admin` หรือคลิกปุ่ม "เข้าสู่ระบบ" (ถ้ามี)
- ระบบจะใช้ Authentication ของ Supabase คุณสามารถสร้าง User ได้ในหน้า **Authentication** บน Supabase Dashboard หรือเปิดระบบ Sign up ไว้

## การนำขึ้นใช้งานจริง (Vercel)
1. สมัคร/เข้าสู่ระบบ **Vercel** (https://vercel.com)
2. เชื่อมต่อกับ Git Repository ของคุณ
3. ในหน้าตั้งค่าโปรเจคบน Vercel ให้ไปที่ **Environment Variables**
4. เพิ่มค่า `VITE_SUPABASE_URL` และ `VITE_SUPABASE_PUBLISHABLE_KEY` เหมือนในไฟล์ .env
5. กด Deploy

---
พัฒนาโดย: [ชื่อของคุณ/เครดิต]
ตกแต่งและแก้ไขเพิ่มเติมได้ตามต้องการ!
