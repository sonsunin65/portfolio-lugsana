# คู่มือการติดตั้งและใช้งาน Teacher Portfolio Template (ฉบับละเอียด)

คู่มือนี้จัดทำขึ้นเพื่อครูและบุคลากรทางการศึกษาที่ได้รับลิงก์ GitHub ของโปรเจคนี้ และต้องการนำไปสร้างเป็นเว็บไซต์ Portfolio ของตนเอง โดยไม่ต้องเขียนโค้ดเองทั้งหมด

---

## สิ่งที่ต้องเตรียม (Prerequisites)
ก่อนเริ่มทำตามคู่มือ ให้สมัครสมาชิกบริการเหล่านี้ให้เรียบร้อย (ฟรีทั้งหมด):
1.  **GitHub Account** (https://github.com/signup) - สำหรับเก็บโค้ดเว็บไซต์
2.  **Supabase Account** (https://supabase.com/dashboard/sign-up) - สำหรับเป็นฐานข้อมูลเก็บข้อความและรูปภาพ
3.  **Vercel Account** (https://vercel.com/signup) - สำหรับนำเว็บไซต์ขึ้นออนไลน์

---

## ขั้นตอนที่ 1: นำโค้ดเข้าบัญชี GitHub ของคุณ (Fork)

1.  เปิดลิงก์ GitHub ของโปรเจคต้นฉบับที่คุณได้รับ
2.  มองหาปุ่ม **"Fork"** ที่มุมขวาบนของหน้าจอ แล้วคลิก
3.  ตั้งชื่อ Repository Name ตามต้องการ (เช่น `my-teacher-portfolio`)
4.  กดปุ่ม **"Create fork"**
5.  ตอนนี้คุณจะได้สำเนาของโปรเจคมาอยู่ใน GitHub ของคุณเองแล้ว

---

## ขั้นตอนที่ 2: ตั้งค่าฐานข้อมูลบน Supabase

ขั้นตอนนี้สำคัญมาก พอร์ตโฟลิโอของคุณต้องมีที่เก็บข้อมูล (ข้อความ, รูปภาพ)

1.  **สร้างโปรเจคใหม่**:
    - ไปที่ [Supabase Dashboard](https://supabase.com/dashboard/projects)
    - คลิก **"New project"**
    - ตั้งชื่อ (Name) และรหัสผ่าน (Database Password) *จดรหัสผ่านเก็บไว้*
    - เลือก Region เป็น **Singapore** (ใกล้ไทยที่สุด โหลดเร็ว)
    - กด **"Create new project"** และรอประมาณ 1-2 นาทีจนเสร็จ

2.  **รันคำสั่งสร้างตาราง (Run SQL)**:
    - ในหน้า Dashboard ของโปรเจค ให้ดูเมนูด้านซ้าย เลือกไอคอน **SQL Editor**
    - คลิก **"+ New query"** (หรือแถบเปล่า ๆ)
    - กลับไปที่ GitHub ของคุณ เปิดไฟล์ชื่อ `setup.sql`
    - **Copy** โค้ดทั้งหมดในไฟล์ `setup.sql`
    - นำมา **Paste** ใส่ในช่อง SQL Editor ของ Supabase
    - กดปุ่ม **"Run"** (สีเขียวมุมขวาล่างของกล่อง)
    - *ผลลัพธ์*: หากขึ้น Success หมายถึงสร้างตารางและระบบเก็บไฟล์เสร็จสมบูรณ์

3.  **เตรียมค่าเชื่อมต่อ (API Keys)**:
    - ไปที่เมนู **Settings** (รูปฟันเฟืองด้านซ้ายล่าง) -> เลือก **API**
    - คุณจะเห็นค่า 2 ตัวที่ต้องใช้ (ห้ามแชร์ให้คนอื่น):
        1.  **Project URL** (เช่น `https://xyz.supabase.co`)
        2.  **Project API Keys (anon public)** (รหัสยาวๆ)
    - *เปิดหน้านี้ทิ้งไว้ หรือ Copy เก็บไว้ก่อน*

---

## ขั้นตอนที่ 3: นำเว็บไซต์ขึ้นออนไลน์ด้วย Vercel

1.  ไปที่ [Vercel Dashboard](https://vercel.com/dashboard)
2.  คลิกปุ่ม **"Add New..."** -> **"Project"**
3.  คุณจะเห็นรายชื่อ Repository จาก GitHub ของคุณ ให้เลือกโปรเจคที่คุณ Fork มา แล้วกด **"Import"**
4.  ในหน้า **Configure Project**:
    - **Framework Preset**: เลือกเป็น `Vite` (ปกติระบบจะเลือกให้อัตโนมัติ)
    - **Root Directory**: ปล่อยไว้เป็น `./` (ปกติ)
    - **Environment Variables** (สำคัญมาก!): คลิกเพื่อขยายเมนูนี้
        - *หมายเหตุ: ขั้นตอนนี้คือการตั้งค่าแทนไฟล์ .env (ไม่ต้องสร้างไฟล์ .env ในเครื่อง)*
        - ให้ใส่ค่า 2 บรรทัดนี้ (Copy จาก Supabase ในขั้นตอนที่ 2 มาใส่):
            1.  Name: `VITE_SUPABASE_URL`
                Value: *(วาง Project URL จาก Supabase)*
            2.  Name: `VITE_SUPABASE_PUBLISHABLE_KEY`
                Value: *(วาง anon public key จาก Supabase)*
5.  กดปุ่ม **"Deploy"**
6.  รอสักครู่ ระบบจะทำการสร้างเว็บไซต์และปล่อยขึ้นออนไลน์
7.  เมื่อเสร็จสิ้น คุณจะได้ลิงก์เว็บไซต์ (Domain) เช่น `my-portfolio.vercel.app` สามารถคลิกเข้าไปดูได้เลย!

---

## ขั้นตอนที่ 4: การเข้าใช้งานระบบจัดการ (Admin)

เมื่อเว็บไซต์ออนไลน์แล้ว คุณสามารถเข้าไปแก้ไขข้อมูลได้ทันที:

1.  เข้าเว็บไซต์คุณ แล้วเติม `/admin` ต่อท้าย URL (เช่น `my-portfolio.vercel.app/admin`)
2.  เนื่องจากเป็นครั้งแรก ระบบอาจจะให้ **Sign Up** (สมัครบัญชีผู้ดูแล) หรือ **Login**
    - *หมายเหตุ*: หากใน Supabase ยังไม่ได้ตั้งค่าปิดการสมัคร ใครก็สมัครได้ ดังนั้นแนะนำให้:
        - สมัครบัญชีของคุณเองก่อน 1 บัญชี
        - กลับไปที่ Supabase -> Memu **Authentication** -> **Providers** -> ปิดการใช้งาน **Email Signup** (ถ้าต้องการจำกัดสิทธิ์)
3.  เมื่อเข้าสู่ระบบ Admin ได้แล้ว คุณสามารถ:
    - **แก้ไขข้อมูลส่วนตัว**: เปลี่ยนชื่อ, รูปโปรไฟล์, ข้อความแนะนำตัว
    - **จัดการผลงาน**: เพิ่ม/ลบ รูปภาพกิจกรรม, เกียรติบัตร, การประเมิน PA
    - **เปลี่ยนธีมสี**: แก้ไขสีพื้นหลังของ icon ต่างๆ ได้ตามเมนูที่มีให้

---

## ขั้นตอนที่ 5: (ตัวเลือกเสริม) การรันเว็บไซต์บนเครื่องตัวเอง

หากคุณต้องการแก้ไขโค้ดที่เครื่องของคุณ (Local Development) ให้ทำตามขั้นตอนนี้:

1.  **Clone โปรเจค**:
    ```bash
    git clone https://github.com/YOUR_USERNAME/my-teacher-portfolio.git
    cd my-teacher-portfolio
    ```
2.  **จัดการไฟล์ Environment**:
    - ในโฟลเดอร์โปรเจค จะมีไฟล์ชื่อ `.env.example`
    - ให้เปลี่ยนชื่อเป็น `.env`
    - เปิดไฟล์ `.env` แล้วนำค่า **Project URL** และ **API Key** จาก Supabase มาใส่
    ```
    VITE_SUPABASE_URL=ใส่_URL_ที่นี่
    VITE_SUPABASE_PUBLISHABLE_KEY=ใส่_Key_ที่นี่
    ```
3.  **ติดตั้งและรัน**:
    ```bash
    npm install
    npm run dev
    ```
    เว็บไซต์จะเปิดขึ้นที่ `http://localhost:8080`

---

## คำถามที่พบบ่อย (FAQ)

**Q: จะแก้ไขข้อความ "Footer" หรือ Copyright อย่างไร?**
A: ไปที่หน้า Admin ส่วน "ข้อมูลโปรไฟล์" จะมีช่องให้ใส่ข้อความ Footer ครับ

**Q: อัปโหลดรูปแล้วไม่ขึ้น?**
A: ลองเช็คที่ Supabase เมนู **Storage** ว่ามี Buckets (profile, works, etc.) ถูกสร้างขึ้นหรือไม่ ถ้าไม่มี ให้ลองรันไฟล์ `setup.sql` อีกครั้ง

**Q: อยากแก้โค้ดเพิ่มเอง (Advance)?**
A: คุณสามารถ Clone โปรเจคจาก GitHub ลงมาที่เครื่องคอมพิวเตอร์ ติดตั้ง Node.js และใช้คำสั่ง `npm install` แล้ว `npm run dev` เพื่อแก้ไขในเครื่องได้เลย เมื่อแก้เสร็จก็ `git push` ขึ้น GitHub แล้ว Vercel จะอัปเดตหน้าเว็บให้อัตโนมัติ

---
ขอให้สนุกกับการทำ Portfolio ครับ! 🎉
